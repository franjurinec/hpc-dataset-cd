import s3
import os

def create_s3_dataset_record():
    # metadata = TODO
    # record = create_record_draft()
    # s3.upload_outputs(os.environ["DATA_OUT_DIR"], f'{os.environ["S3_DATA_BASE"]}/{record.id}')
    # submit(record)
    return None
